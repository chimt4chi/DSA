#include <iostream>
using namespace std;

int add(int arr[], int start, int &end, int &size){
// int size = sizeof(arr[0]) / sizeof(arr);
    
        int sum;
    for(int i=0; i<=size/2; i++){
        sum = arr[i] + arr[size - i - 1];
        if(arr[i] == arr[size - i - 1]){
            cout << arr[i];
        }else {
            cout << sum << endl;
        }
    }
        // cout << sum << endl;
    
    return 0;
};

int main() {
    // Write C++ code here
    // std::cout << "Try programiz.pro";
    int arr[] = {1,2,3,4,7};
    int start, end, size;
    size = sizeof(arr) / sizeof(arr[0]);
    end = size - 1;
    add(arr, start, end, size);

    return 0;
}